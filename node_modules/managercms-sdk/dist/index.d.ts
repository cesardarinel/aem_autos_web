export declare class ManagerCMSError extends Error {
    status: number;
    data?: any | undefined;
    constructor(status: number, message: string, data?: any | undefined);
}
export interface WebsiteInfo {
    id: number;
    name: string;
    domain: string;
}
export interface ContentType {
    id: number;
    name: string;
    slug: string;
}
export interface Entry {
    id: number;
    created_at: string;
    updated_at: string;
    data: Record<string, any>;
}
export interface GetEntriesOptions {
    pageSize?: number;
    page?: number;
    filters?: Record<string, string | number | boolean>;
    ordering?: string;
    search?: string;
}
export declare class ManagerCMS {
    private apiUrl;
    private token;
    constructor(token: string, apiUrl?: string);
    private request;
    getWebsiteInfo(): Promise<WebsiteInfo>;
    getContentTypes(): Promise<ContentType[]>;
    getEntries<T = Entry>(modelSlug: string, options?: GetEntriesOptions): Promise<T[]>;
    getEntry<T = Entry>(modelSlug: string, id: number | string): Promise<T>;
    createEntry<T = Entry>(modelSlug: string, data: any): Promise<T>;
    updateEntry<T = Entry>(modelSlug: string, id: number | string, data: any): Promise<T>;
    deleteEntry(modelSlug: string, id: number | string): Promise<void>;
}
